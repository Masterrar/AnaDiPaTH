<Window x:Class="NIR.Views.MainView"
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="588" Width="1050"
        
        >
    <!--<Window.CommandBindings>

        <CommandBinding Command="NIR:Commands.Pointer" Executed="cmd_Pointer" />
        <CommandBinding Command="NIR:Commands.AddPolyline" Executed="cmd_AddPolyline" />
        
    </Window.CommandBindings>-->
    <DockPanel Margin="0,0,0,526">
        <Expander Header="Expander" Canvas.Left="19" Canvas.Top="39" DockPanel.Dock="Top">
            <Grid Background="#FFE5E5E5">
                <ToggleButton x:Name="DoLine"   Width="60" Height="30" HorizontalAlignment="Left" Padding="10" Content="Линия" Checked="cmd_AddPolyline" />
            </Grid>
        </Expander>
        <Grid Margin="0,0,0,-23" Width="1042">
            <Grid.RowDefinitions>
                <RowDefinition Height="16*"/>
                <RowDefinition Height="243*"/>
                <RowDefinition Height="20*"/>
            </Grid.RowDefinitions>
            <Canvas Grid.Row="1"  x:Name="DrawCanvas" ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="10"  />
        </Grid>
    </DockPanel>
</Window>
